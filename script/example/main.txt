// tanucc-script[0.1]
use std::{*};


fn main(){
    //let f = Std::file();
    let file = @open(@to_path("example/main.txt"));
    let content = @read_file(file);
    //@play_music("test-dir/pokemon-bw-syouri-mokuzen.wav");
}
